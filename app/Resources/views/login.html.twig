<!--<div class="btn-group pull-right">-->
  {% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <a title="User Login" class="pull-right top_menu" href="#">
      <i class="icon-user icon-white"></i>
      <span>{{ app.user.username }}</span>
    </a>
    <a title="Social Chat" class="pull-right top_menu" href="{{ path('bioversity_site_structure_online_chat') }}">
      <i class="icon-comments icon-white"></i>
    </a>
    <!--
    <button class="btn btn-info dropdown-toggle" data-toggle="dropdown">
      <span class="caret"></span>
    </button>
    -->
    <!--
    <ul class="dropdown-menu" style="width: 240px">
      {% if is_granted('ROLE_ADMIN') %}
        <li>
          <a href="{{ path('bioversity_user_homepage') }}">Manage User</a>
        </li>
      {% endif %}
      {% if is_granted('ROLE_ONTOLOGY') %}
        <li>
          <a href="{{ path('bioversity_ontology_browse_slider') }}">Manage Node</a>
        </li>
      {% endif %}
      <li><a href="#">Profile</a></li>
      <li><a href="{{ path('b_s_logout') }}">Sign Out</a></li>
    </ul>
    -->
  {% else %}
    <a class="pull-right top_menu" href="{{ path('b_s_login_path') }}">
      <span>Login</span>
    </a>
    <!--
    <button class="btn btn-info dropdown-toggle" data-toggle="dropdown">
      <span class="caret"></span>
    </button>
    <ul class="dropdown-menu" style="width: 240px">
      <li><a href="{{ path('b_s_registration') }}">Registration</a></li>
    </ul>
    -->
  {% endif %}
<!--</div>-->
